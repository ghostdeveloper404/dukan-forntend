/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['imgs.search.brave.com', 'picsum.photos'], // Add any domains you use
    },
  };
  
  export default nextConfig;
  